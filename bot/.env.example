# Copy to .env and fill in. Never commit the real .env.
BOT_TOKEN=123456789:ABCdefGhIJKlmNoPQRsTUVwxyz   # from @BotFather
OWNER_USERNAME=ibrokh1movv7                       # owner without the @
OWNER_ID=                                         # owner's numeric id (run /myid in the bot)
# DB_PATH=/data/aquality_bot.db                   # mount a volume here in production

# ── Telegram bot public username (shown in linking instructions on the website) ──
BOT_USERNAME=aqualityHL_bot                       # without the @

# ── Supabase sync (optional — enables account linking + subscription sync) ──
SUPABASE_URL=https://uhyomjdsswasmlycpoyh.supabase.co
BOT_SYNC_SECRET=                                  # generate a strong secret; set same value in Supabase edge function env as BOT_SYNC_SECRET
SITE_URL=https://aquality-hl.netlify.app
