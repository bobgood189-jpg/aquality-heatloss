# Copy to .env and fill in. Never commit the real .env.
BOT_TOKEN=123456789:ABCdefGhIJKlmNoPQRsTUVwxyz   # from @BotFather
OWNER_USERNAME=ibrokh1movv7                       # owner without the @
OWNER_ID=                                         # owner's numeric id (run /myid in the bot)
# DB_PATH=/data/aquality_bot.db                   # mount a volume here in production

# ── Supabase integration (optional but recommended) ──────────────────────────
# Enables: Telegram account linking, Supabase subscription sync, registration flow
SUPABASE_URL=https://ocrgpmlhtjghiamhbrhv.supabase.co
SUPABASE_SERVICE_KEY=                             # service_role key from Supabase Dashboard → Settings → API
BOT_USERNAME=AqualityHLBot                       # without the @
SITE_URL=https://aquality-hl.netlify.app          # website URL shown in /link instructions

# ── Paywall ──────────────────────────────────────────────────────────────────
# PAYWALL=1                                       # uncomment to require subscription for full calc

# ── Subscription shop ────────────────────────────────────────────────────────
PAY_CARD_NUMBER=0000 0000 0000 0000               # card number shown to customers
PAY_CARD_NAME=Имя Фамилия                         # card holder name
ADMIN_CHAT_ID=                                    # Telegram ID to receive order notifications (defaults to OWNER_ID)
