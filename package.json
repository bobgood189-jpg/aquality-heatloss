{
  "name": "aquality-html",
  "private": true,
  "description": "Aquality heat-loss calculator — static site (Netlify)",
  "scripts": {
    "build:css": "tailwindcss -i src/tailwind.input.css -o assets/tailwind.css --minify",
    "watch:css": "tailwindcss -i src/tailwind.input.css -o assets/tailwind.css --watch"
  },
  "dependencies": {
    "@supabase/server": "^1.2.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
