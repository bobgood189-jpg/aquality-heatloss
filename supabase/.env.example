# Локальная разработка Edge Functions (supabase functions serve --env-file supabase/.env).
# В проде секреты задаются через `supabase secrets set ...` — этот файл не деплоится.
# Скопировать в supabase/.env и заполнить. НЕ коммитить .env с реальным паролем.

ESKIZ_EMAIL=bobgood189@gmail.com
ESKIZ_PASSWORD=
ESKIZ_FROM=4546
