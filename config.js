/* ============================================================================
 * Aquality | WaterPro — конфигурация Supabase (ШАБЛОН)
 * ============================================================================
 *
 * ВАЖНО про single-file: основной конфиг живёт ВВЕРХУ index.html
 * (блок `window.AQ_CONFIG = { ... }` сразу после подключения CDN).
 * Этот файл — удобный шаблон/памятка. Чтобы включить бэкенд:
 *
 *   1) Создайте проект на https://supabase.com
 *   2) Settings → API → скопируйте «Project URL» и «anon public» ключ
 *   3) Откройте index.html, найдите `window.AQ_CONFIG` (верх файла)
 *      и вставьте туда свои значения ВМЕСТО плейсхолдеров YOUR_SUPABASE_*
 *   4) Выполните supabase/schema.sql в Supabase → SQL Editor
 *
 * Используется ТОЛЬКО anon (public) ключ — он предназначен для клиента и
 * защищён политиками RLS. НИКОГДА не вставляйте service_role ключ в клиент.
 *
 * Альтернатива (если хотите держать ключи отдельным файлом):
 * раскомментируйте строку ниже и добавьте в index.html ПЕРЕД supabase-js:
 *   <script src="config.js"></script>
 * (по умолчанию index.html этот файл НЕ подключает, чтобы не было 404
 *  на статике, когда файл не залит.)
 * ========================================================================== */

window.AQ_CONFIG = {
  SUPABASE_URL: 'YOUR_SUPABASE_URL',
  SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY'
};
